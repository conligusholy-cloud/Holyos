# Soubory/složky, které se NEMAJÍ nahrávat na Railway při `railway up`.
# (kód + build se nahrává; tohle jsou velké nepotřebné věci)

# Závislosti — Railway si je instaluje sám (npm install při buildu).
# BEZ tohohle se nahrávaly stovky MB a upload na pomalé síti timeoutoval.
node_modules/
.git/

# Podklady (fotky/videa pro práci, ne pro server)
podklady/

# Lokální data — na Railway je persistent volume /app/data
data/

# Zálohy, buildy APK a nástroje
backups/
*.apk
*.jar

# Logy a velké dumpy
*.log
goods-*.json

# Klientské aplikace — server je nenasazuje/neservíruje (nasazují se zvlášť).
# BEZ tohohle mělo clients/cad-bridge ~3,7 GB (staré .msi/.exe buildy) a upload timeoutoval.
clients/cad-bridge/
clients/velin-mobile/
# PWA sklad: server servíruje jen dist/, node_modules netřeba nahrávat
clients/pwa-sklad/node_modules/

.puppeteer-cache/
